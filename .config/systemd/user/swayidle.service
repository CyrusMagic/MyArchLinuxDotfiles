[Unit]
Description=Idle manager for niri (sleep hook only)
PartOf=graphical-session.target
After=graphical-session.target
Requisite=graphical-session.target

[Service]
ExecStart=/usr/bin/swayidle -w before-sleep '/home/cyrus/.local/bin/gtklock-random'
Restart=on-failure

[Install]
WantedBy=graphical-session.target

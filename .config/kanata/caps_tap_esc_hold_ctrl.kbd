(defcfg process-unmapped-keys yes)

(defsrc
  caps
)

(defalias
  ;; CapsLock: tap=Esc, hold=Hyper (Ctrl+Alt+Shift+Super)
  caps (tap-hold 200 200 esc (multi lctl lalt lsft lmet))
)

(deflayer default
  @caps
)
